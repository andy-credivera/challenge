/**
 * 🚨 INSTRUCTIONS FOR CANDIDATE:
 * 
 * Write a single SQL query that returns the names ('name') and salaries ('salary')
 * of all employees who work in the 'Engineering' department.
 * 
 * Sort the results by salary in descending order (highest salary first).
 */

export const candidateQuery = `
  -- Write your SQL query below this line --
  
`;
