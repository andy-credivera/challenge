# challenge

**A)** Open questions/README.md and provide the answers

**B)** Open react-challenge-1/README.md for coding challenge.  To run, execute `npm run start-react` in the terminal

**C)** Open sql-challenge-1/challenge.js for sql challenge.  To run, execute `npm run start-sql` in the terminal